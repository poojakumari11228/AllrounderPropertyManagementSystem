<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.firebaseconnection">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/bb_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".Home" />
        <activity android:name=".FindJob" />
        <activity android:name=".FindArchitects" />
        <activity android:name=".AddProperty"/>
        <activity android:name=".FindProperty"/>
        <activity android:name=".Prop_Details"/>
        <activity android:name=".SignupActivity"/>
        <activity android:name=".ArchitectDetails">
        </activity>
    </application>

</manifest>